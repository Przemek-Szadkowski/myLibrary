<div class="books_wrapper"></div>
<h2>My books</h2>
<ul>
    {{#each books}}
        <li>"{{this.title}}" / {{this.author}} / {{this.year}} / cover: {{this.cover}}
            <a href="/book/{{this.id}}">Edit</a>
            <form method="POST" action="/book/{{this.id}}?_method=DELETE"><button type="submit">Delete</button></form>
        </li>
    {{/each}}
</ul>